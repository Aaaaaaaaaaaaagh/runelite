This is a fork of runelite that runs some code that makes it easier for a particular bot API to interface with this client. It's not open source, but you're free to ask for parts of my implementation.
